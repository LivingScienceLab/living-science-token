"""Living Science Lab data pipeline.

A minimal, dependency-free data-engineering pipeline: ingest -> transform ->
output. The output is an "AI-ready" dataset plus a provenance manifest (the
seed of the IP-asset model the LSL token will gate access to).
"""

__version__ = "0.1.0"
